<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.app_conversao">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Hexadecimal_Decimal"></activity>
        <activity android:name=".Hexadecimal_Binario" />
        <activity android:name=".Binario_Hexadecimal" />
        <activity android:name=".Binario_Decimal" />
        <activity android:name=".Decimal_Hexadecimal" />
        <activity android:name=".Decimal_Binario" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>